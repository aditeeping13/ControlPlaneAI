# ControlPlane.ai

Backend MVP for ControlPlane.ai Hackathon.

## Getting Started

1. `pip install -r requirements.txt`
2. `cp .env.example .env` and fill in API keys
3. `uvicorn backend.main:app --reload`
